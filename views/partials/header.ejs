<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= metaTitle || (title + ' | Sohan Blog') %></title>
  <meta name="description" content="<%= metaDescription || 'A modern blog' %>" />
  <link rel="canonical" href="<%= baseUrl + currentPath %>" />
  <meta property="og:title" content="<%= metaTitle || title %>" />
  <meta property="og:description" content="<%= metaDescription || 'A modern blog' %>" />
  <meta property="og:url" content="<%= baseUrl + currentPath %>" />
  <meta property="og:type" content="article" />
  <% if (ogImage) { %>
    <meta property="og:image" content="<%= ogImage %>" />
  <% } %>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-slate-950 text-slate-100">
  <header class="border-b border-slate-800 bg-slate-950/70 backdrop-blur">
    <div class="mx-auto max-w-6xl px-4 py-4 flex items-center justify-between">
      <a class="text-lg font-semibold tracking-wide" href="/">Sohan Blog</a>
      <nav class="flex items-center gap-4 text-sm">
        <a class="hover:text-slate-300" href="/blog">Blog</a>
        <a class="hover:text-slate-300" href="/rss.xml">RSS</a>
        <% if (user) { %>
          <a class="hover:text-slate-300" href="/admin">Admin</a>
        <% } %>
      </nav>
    </div>
  </header>
  <main class="mx-auto max-w-6xl px-4 py-10">

