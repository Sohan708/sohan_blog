<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= metaTitle || (title + ' | Sohan Blog') %></title>
  <meta name="description" content="<%= metaDescription || 'A modern blog' %>" />
  <link rel="canonical" href="<%= baseUrl + currentPath %>" />
  <meta property="og:title" content="<%= metaTitle || title %>" />
  <meta property="og:description" content="<%= metaDescription || 'A modern blog' %>" />
  <meta property="og:url" content="<%= baseUrl + currentPath %>" />
  <meta property="og:type" content="article" />
  <% if (ogImage) { %>
    <meta property="og:image" content="<%= ogImage %>" />
  <% } %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Bengali:wght@400;500;600;700&family=Noto+Serif+Bengali:wght@500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles/main.css" />
</head>
<body>
  <header class="site-header">
    <div class="container header-top">
      <span class="header-kicker">Independent news desk</span>
      <span class="header-date"><%= new Date().toDateString() %></span>
    </div>
    <div class="container header-brand">
      <a class="logo" href="/">Sohan Blog</a>
    </div>
    <div class="container header-nav">
      <nav class="site-nav">
        <a href="/">Home</a>
        <a href="/blog">Latest</a>
        <a href="/rss.xml">RSS</a>
        <% if (user) { %>
          <a href="/admin">Admin</a>
        <% } %>
      </nav>
    </div>
  </header>
  <main class="site-main container">
